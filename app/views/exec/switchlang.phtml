<?php
if (session_status() == PHP_SESSION_NONE) {
	session_start();
	
  }

$text = isset($this->viewData['lang']) ? $this->viewData['lang'] : null;
$_SESSION['lang'] = $text ?? "en";
echo trim($_SESSION['lang']) ;